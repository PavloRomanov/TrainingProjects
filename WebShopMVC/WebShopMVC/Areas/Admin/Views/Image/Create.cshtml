@model WebShop.Model.ViewModel.ImageViewModel

@{
    ViewBag.Title = "Create";
}

<h2>Create image</h2>




@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>???????????????????</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.ImageId, htmlAttributes: new { @class = "control-label col-md-2" })
        </div>

        <div class="form-group">
           @* @Html.CheckBox("", htmlAttributes: new { @class = "control-label col-md-2" })*@
            </div>

        @*<img width=600 height="400" scr="@Url.Action("Image","Image",new {Model.ProductId})"/>*@

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "List")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}